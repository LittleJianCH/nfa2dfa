const element = document.createElement("h1");
const network = document.createElement("div");

element.innerHTML = "Welcome to NFA2DFA!";

network.id = "network";

document.body.appendChild(element);
document.body.appendChild(network);
